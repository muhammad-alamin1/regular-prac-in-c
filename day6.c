#include <stdio.h>

int main(void)
{
    system("cls");

    int i, j, k, num, N, sum = 0, first = 0, second = 1, count = 0;
    float ave;

    printf("Enter N: ");
    scanf("%d", &N);

    for(i = 1; i <= N; i++)
    {
        if(N % )
    }




    getch();
}
